# Testing dependencies for FantaSanremo backend
# Now managed in pyproject.toml - use: uv sync --extra dev

# Core testing framework
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1

# HTTP testing
httpx==0.28.1

# Database testing
# (Already in main requirements: sqlalchemy, etc.)

# Coverage reporting
coverage==7.3.2

# Code quality
pytest-clarity==1.0.1
pytest-xdist==3.5.0  # Parallel test execution

# Mocking and fixtures
pytest-mock==3.12.0
freezegun==1.4.0  # Mock time in tests

# Test output formatting
pytest-sugar==0.9.7
pytest-emoji==0.2.0
